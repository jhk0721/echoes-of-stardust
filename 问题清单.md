# 《星尘回响》问题排查清单

> 排查日期：2026-08-17 ｜ 排查范围：设定集 / core 后端 / native 桥接 / assets 素材 / tools 脚本
> 排查方式：静态通读 + 运行 4 个官方自检脚本（Python 3.11.0 + pygame 2.6.1）
> 自检结果：smoke ✅ ｜ story ✅ ｜ worlds ✅（16 星球全 PASS）｜ **map ❌ 失败**

---

## 一、后端代码问题（有真 bug）

### 🔴 1. BGM 采样率写死，导致 BGM 倍速播放、音调升高一个八度（最隐蔽的坑）
- 位置：`tools/generate_assets.py`
- `gen_bgm()` 里用 `rate = 22050` 采样生成 16 秒的琶音，但 `_save_wav()` 写死了 `w.setframerate(44100)`。
- 后果：352800 个样本被当成 44100Hz 播放 → **实际 8 秒就放完，速度快一倍、音高高一个八度**。你听到的 BGM 一直是"加速版"。
- 而且 `gen_bgm` 注释写"16 秒循环"，`gen_audio` 注释写"8 秒循环"，自己跟自己打架。
- 修法：`_save_wav` 加一个 `rate` 参数，`gen_bgm` 传 22050；或者干脆把 BGM 也用 44100 采样。

### 🔴 2. 共鸣不增伤，和设定集不符
- 位置：`core/main.py` `BattleScene.fire()` / `update()`
- 设定集明确写"精准命中触发共鸣——**伤害翻倍**"。但代码里共鸣（`res == "hit"`）只加灵韵、闪白、弹 toast，**音波弹伤害永远是固定 10 点**（`self.hp -= 10`），跟共不共鸣没关系。
- 后果：共鸣机制只剩下"攒灵韵"一个功能，"共鸣=爆发"的核心爽点没落地。

### 🟠 3. 聆听（U 键）功能残缺
- 设定集：长按 U → 时间降到 30%、**暴露敌人节奏型**、且"聆听时不能弹奏"。
- 代码现状：`self.listening` 只做了时间减速（`ts=0.3`）。节奏型 `rn` 在 `draw()` 里**无论是否聆听都一直显示在敌人头顶**——"洞察破绽"的设计形同虚设；而且聆听时照样能 `fire()` 弹奏，没有禁用。
- 修法：节奏型只在 `self.listening` 时绘制；聆听中屏蔽 `fire`。

### 🟠 4. 发射方向实现与教程文案矛盾
- 设定集/`planet.py` 教程写的是"朝**鼠标**指向发射"。
- 代码实际是 `self.last_dir`（最后一次 WASD 移动方向）发射，鼠标根本不参与。
- 后果：教程在教一个不存在的操作，玩家会懵。二选一：要么真做鼠标瞄准，要么把教程文案改成"朝移动方向弹奏"。

### 🟠 5. `map_test.py` 自检失败（已复现）
- 报错：`AssertionError: 探索点未全部完成`，存档 `pois: []`。
- 根因：测试脚本是 **WalkScene（局部探索场景）加入之前写的**，主循环只处理 `MapScene / StoryScene / BattleScene` 三种，进了 `WalkScene` 就只会空转 `frames(2)`，永远走不到 NPC 对话，导致一个探索点都没完成。
- 本质是**测试没跟上代码演进**，README 却还写着"map_test 探索地图全流程自检"。

### 🟡 6. 其它小问题
- `scene.py` `_advance_scene()` 末尾：`nxt` 为空时 `self._save()` 被调了两次（无害但冗余）。
- `scene.py` `_choose()` 碎片名写死 `"再见的见"`，只适配浅海星，不通用。
- `save.py` 的 `unlocked` 字段形同虚设：每次存档都被覆盖成 `[当前星球]`，`PlanetSelectScene` 也根本不读它，16 颗星球无条件全可降落——**解锁系统没实现**。
- `audio.py`：`StoryScene.update` 里 `if not mixer.get_busy(): play("whisper")` —— 但 bgm 一直在循环、`get_busy()` 恒为 True，**whisper 永远不触发**，是死逻辑。
- `native/bridge.py` 的 `judge_sequence()` 写了但全项目无人调用，死代码。
- `planet_shader.py` `render_python` 里 `_hash21(int(...))` 取整，和 GLSL 的 float hash 结果并不一致，注释却宣称"结果一致"。
- 项目根目录有个 0 字节的 `$null` 文件——PowerShell 重定向 `$null` 时的经典翻车产物，垃圾文件，可删。

---

## 二、剧情设计问题（设定集 vs 实现）

### 🔴 1. 只有浅海星是"完整剧情"，其余 15 颗星球全是模板
- 浅海星《船》在 `core/story/planet.py` 里有完整的 `QIANHAI`（tutorial/open/修网/找桨/战斗/贝壳/夜谈/告别/挽歌 9 个节点，含互动、选项、战斗）。
- 但另外 4 个**主线章节**（暴雨孤城/圣母院/降维废墟/终章之路）在 `worlds.py` 里**只有一个 `talk` 节点、几句对话**，没有碎片收集、没有互动、没有选项、没有战斗。
- 11 颗自由探索星球更惨：`MapScene` 对非浅海星会自动合成 3 个 POI——对话/碎片/挽歌，其中"碎片"和"挽歌"全是**同一段模板文案**（`scene.py` 里 `setdefault` 的兜底文本）。设定集里福贵的破布鞋、小王子的围巾、桑提亚哥的鱼骨……这些专属内容**一个都没落地**。
- **README 里 `[x] 主线五章` 这个勾是虚标的。**

### 🔴 2. 终章"四选一"抉择没实现
- 设定集第十一章给了 4 个结局（哈姆雷特/克洛迪斯/未完待续/星尘之路）。
- 代码里 `finale_world` 只有"继续旅行"这一条路的旁白，**没有选项分支**。这是全剧的情感顶点，目前是一段过场文字。

### 🔴 3. 隐藏 BOSS"造物主"+ 洛水桥边（赵馨语）完全未实现
- 设定集第十四章的"造物主"元叙事关卡、第十三章赵馨语的洛水桥彩蛋，代码里没有任何对应实现。README 也诚实标了 `[ ]` 未完成。

### 🔴 4. 铭记旋律"获得方式"全部缺失
- `core/combat/finale.py` 里 10 段铭记旋律的**序列本身**和设定集完全对得上（我逐条核对过 ✅）。
- 但设定集里每段旋律的**获得方式**（电话亭擦 99 次玻璃、浇花 7 天、弹 100 次二向箔……）**一个都没做**。玩家现在只能靠瞎按 8 槽碰运气触发。
- 而且**游戏内没有任何地方告诉玩家这些序列是什么**——没有图鉴、没有提示。README 说"铭记旋律 10 大招"，实际是"10 个没人知道怎么开的大招"。

### 🟠 5. 章节编号大乱（强迫症地狱）
`worlds.py` 的 `title` 字段编号重复冲突：
| 编号 | 撞车的星球 |
|---|---|
| 第三章 | 浅草寺《樱》 与 圣母院《钟》 |
| 第四章 | 卡塞尔学院《茶》 与 降维废墟《向日葵》 |
| 第五章 | 朋友星球《灯》 与 终章之路《选择》 |
- 根因：设定集是"**主线 5 章 + 自由探索 11 星**"两层结构，但代码把 16 颗星拉平成一列重新编号，于是和主线编号撞车。

### 🟠 6. 暴雨孤城实现的是"通关后"状态，不是主线第二章
- 设定集第二章：楚子航**背对站在电话亭里不说话**，玩家收集竹刀/校服/纽扣碎片，他逐渐开口，最后拨通父亲电话听到录音。
- 代码 `storm_city` 里：他**坐在电话亭外台阶上、竹刀横放膝头**（这是设定集"自由探索·通关后再来"的形态），电话亭、碎片收集、拨号全没有。等于把"告别后的重逢"提前当成了初见。

### 🟡 7. 版权/IP 提醒（重要）
- 设定集大量借用有版权的角色：楚子航/夏弥/昂热/路明非/绘梨衣（龙族）、卡西莫多（巴黎圣母院）、福贵（活着）、史铁生（地坛）、桑提亚哥（老人与海）、小王子、斯内普/邓布利多（哈利波特）、三体、流浪地球、斩神、诗云……
- 作为**个人学习/同人作品没问题**，README 也声明了"不商用、不发布"。但**一旦想公开发布或商用，这些角色全是雷**。第 11 段"最后的和弦"是联机隐藏项，目前也没法实现。

---

## 三、素材寻找 / 获取问题

### 🔴 1. 63MB 星露谷物语素材包 = 版权定时炸弹
- `assets/StardewValley-Assets-main.zip`（63MB）+ 已解压的 `assets/stardew/`。
- 星露谷素材版权属 ConcernedApe，**禁止再分发和衍生使用**，连非商用都算灰色。README 的素材许可表里**根本没提它**。
- 而且代码里**完全没用到**（只有 `probe_assets.py` 扫过它）。建议直接删，别留在仓库里。

### 🟠 2. assets 根目录躺着一堆没处理完的 zip
- 11 个 zip 散在 `assets/` 根目录（`037902c594e3d95b.zip` 等一堆哈希命名的）。
- `install_assets.py` 只认特定关键词命名的包并把它们挪去 `_dl/`，这些哈希命名的包匹配不上，就一直堆着占空间。

### 🟠 3. NPC 素材路径是相对路径，换个目录启动就崩
- `map_scene.py` / `walk_scene.py` 里 `NPC_DIR`、`WR`、`SEA_DIR` 全是 `assets/_new/...` 相对路径，`_bg_img` 也只查 `assets/_new`。
- 依赖"当前工作目录必须是项目根"。一旦从别的目录启动或打包成 exe，NPC 图全部加载失败、退化成白色圆圈占位。建议用 `config.BASE` 拼绝对路径。

### 🟡 4. 其它素材卫生问题
- `assets/fonts/` 有 15 个 ttf（三版字 × 5 种语言），代码只用 `zh_hans` 的 3 个，ja/ko/zh_hant/latin 全没用上。
- `harp_real.png` 是 pngimg.com 的 CC BY-NC 素材，README 声明了非商用，但**署名义务是否满足要再确认**。
- 开发文档 `docs/开发文档.md` 提到的 `tools/gen_planets.py`（生成 8 张专属星球 PNG）和 `assets/sprites/planet_0~7.png` **实际不存在**——文档和现状脱节。

---

## 四、运行环境 / 其它

### 🟠 1. `gpu_pref.py` 每次启动都写注册表
- `run.py` 每次运行都会调 `gpu_pref.apply()`，往 `HKCU\...\UserGpuPreferences` 写 `GpuPreference=2`，且**硬编码了"MX150 独显"**。
- 只适配开发者自己的机器，换台电脑毫无意义，还属于"用户无感知的系统设置修改"。建议加开关或只在首次运行时执行。

### 🟠 2. 自检会污染真实存档
- `story_test.py` / `map_test.py` / `worlds_test.py` 直接读写项目根的 `save.json`。跑完测试后，当前 `save.json` 已经是测试残留状态（`planet: b612`、`done: true`），会覆盖你的真实进度。建议测试用独立的临时存档路径。

### 🟡 3. `native/rhythm_core.dll` 未构建
- `bridge.py` 检测不到 DLL，自动降级到纯 Python 实现（设计如此，能跑）。但 C++ 部分实际从没编译过，`rhythm_core.cpp` 目前是"理论存在"。

---

## 五、优先级建议

| 优先级 | 事项 | 为什么 |
|---|---|---|
| P0 | 修 BGM 采样率 bug | 一行改动，但直接影响所有玩家的听感 |
| P0 | 共鸣增伤 + 聆听功能补全 | 战斗核心体验，现在是"假共鸣" |
| P0 | 删除星露谷素材包 | 版权风险最高的单点 |
| P1 | 给其余主线章节补真实剧情节点 | 否则"16 星球"只是 16 个模板 |
| P1 | 终章四选一抉择 | 全剧情感顶点 |
| P1 | 铭记旋律获得方式 + 序列图鉴 | 10 大招现在没人会开 |
| P2 | 修 map_test、解锁系统、章节编号 | 工程质量 |
| P2 | NPC 素材绝对路径、清理冗余 zip/字体 | 健壮性/瘦身 |

---
*兄弟，骨架是真的扎实——主界面那个动态宇宙、粒子星球、五线谱做得很漂亮，浅海星全流程闭环也跑通了。现在的核心矛盾就一句话：设定集是一部 16 星球的宏大叙事，代码目前真正做完的只有 1 颗。先把浅海星这套"节点+互动+战斗+挽歌"的模式复制到另外 4 个主线章节，这游戏就立住了。加油，焦汇坤！🎵*
